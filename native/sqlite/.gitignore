# Source tarballs — fetched by README.md recipe, don't commit
third_party/*.tar.gz

# Extracted upstream source trees — regenerated by README.md recipe.
# We commit only the amalgamation output under amalgamation/.
third_party/sqlcipher-*/
third_party/sqlite-vec-*/
third_party/openssl-*/

# Build outputs (already caught by top-level .gitignore; explicit for clarity)
build/
host-build/
prebuilt/
