# ADOPTION_MATRIX

Directive §16. **55 findings, one classification each, eleven fields each.**

| | |
|---|---|
| Upstream | `https://github.com/jegly/OfflineLLM` |
| Tag / commit | `5.1.1` / `e81091e86013c0605381d15a1ad7276a4be0b92b` |
| Licence | Apache-2.0 (no `NOTICE` file in the tree; `smollm/` is adapted from `shubham0204/SmolChat-Android`, also Apache-2.0) |
| Reviewed | 2026-09-23 |

Every row's `Commit/tag` is `5.1.1` / `e81091e` and every row's `Licence` is `Apache-2.0` unless
stated; both are carried per row as §16 requires. Paths are repository-relative to the tag;
prefix `https://github.com/jegly/OfflineLLM/blob/e81091e86013c0605381d15a1ad7276a4be0b92b/` for a
permalink. Difficulty is **XS** (≤ 5 lines) / **S** (one file) / **M** (a few files + tests) /
**L** (design + multiple modules). Detail for every row is in the document named in the
*Reasoning* cell.

## Counts

| Classification | Count | Ids |
|---|---:|---|
| **ADOPT** | **7** | OL-01, OL-05, OL-07, OL-08, OL-29, OL-38, OL-46 |
| **ADAPT** | **10** | OL-04, OL-09, OL-13, OL-16, OL-17, OL-19, OL-24, OL-27, OL-47, OL-54 |
| **REFERENCE** | **20** | OL-02, OL-03, OL-10, OL-11, OL-15, OL-18, OL-21, OL-23, OL-28, OL-30, OL-31, OL-34, OL-37, OL-39, OL-40, OL-45, OL-50, OL-52, OL-53, OL-55 |
| **REJECT** | **18** | OL-06, OL-12, OL-14, OL-20, OL-22, OL-25, OL-26, OL-32, OL-33, OL-35, OL-36, OL-41, OL-42, OL-43, OL-44, OL-48, OL-49, OL-51 |
| **Total** | **55** | |

---

## 1. The matrix

### 1.1 Backend selection and Vulkan (directive §5, §6)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-01** Restrict `llama_model_params.devices` to CPU+ACCEL when `nGpuLayers <= 0` | `smollm/src/main/cpp/LLMInference.cpp#L137-L149` | 5.1.1 / `e81091e` | Apache-2.0 | **ADOPT** | The 5.1.1 headline fix. Verified still necessary in Skein's *newer* llama.cpp pin: `devices == nullptr` puts the Mali iGPU in `model->devices` regardless of `n_gpu_layers`. `VULKAN_ANALYSIS.md` §1, §3, §6 | **Missing.** `skein_jni.cpp:363-368` / `:426-428` set `n_gpu_layers` only | XS (~12 lines × 2 entry points) | "CPU-only" becomes true; prompt processing stops being copied to the GPU; the in-app CPU path becomes measurable | Must apply to **both** `loadModel` and `loadModelFromFd`; a future load path could miss it (R-1 guards). No behaviour change at `ngl > 0` | **M0** |
| **OL-02** The buffer-type-ordering diagnosis, and `params.no_host` as a second lever | `smollm/src/main/cpp/LLMInference.cpp#L131-L133` (comment) | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | `make_cpu_buft_list` orders *ACCEL → GPU host → CPU extra → CPU*; `no_host = true` suppresses the GPU host entry without touching the device list. Insufficient alone — the backend still joins the scheduler. `VULKAN_ANALYSIS.md` §4 | Not set; default `false` | — | Knowledge: a belt-and-braces option and an explanation of the repack interaction | Choosing `no_host` *instead of* OL-01 would fix one of three effects and look like a fix | — |
| **OL-03** `op_offload` sends any batch ≥ 32 tokens to Vulkan | llama.cpp `ggml-vulkan.cpp#L19823-L19827`, `#L19986` (upstream, surfaced by OfflineLLM's comment) | llama.cpp `b29c606` | MIT | **REFERENCE** | Skein's `PROMPT_BATCH_TOKENS = 512` is 16× the threshold. `GGML_OP_OFFLOAD_MIN_BATCH` is a no-rebuild A/B lever. `VULKAN_ANALYSIS.md` §3 | `op_offload` left at default `true` | — | Makes R-2 writable without new native API; names the hybrid behaviour Skein has by accident | Relying on the env var in production would be fragile — it is a test lever only | — |
| **OL-04** Retry once on CPU when a GPU-offload load fails | `app/src/main/java/com/jegly/offlineLLM/ai/InferenceEngine.kt#L63-L80` | 5.1.1 / `e81091e` | Apache-2.0 | **ADAPT** | 12 lines that keep the app usable on a flaky Vulkan driver; the remedy for PocketPal PP-55. `VULKAN_ANALYSIS.md` §7 | `InferenceService` maps the `LlamaException` to an `ErrorCode` and stops | S | No dead-end on a driver that refuses allocation | A **silent** downgrade hides a real problem — the retry belongs in `:app`, above the IPC boundary, and must be visible in the diagnostic surface | M1 |
| **OL-05** `getBackendReport()` / `getGpuDeviceName()` over `ggml_backend_get_features` | `smollm/src/main/cpp/smollm.cpp#L60-L140` | 5.1.1 / `e81091e` | Apache-2.0 | **ADOPT** (adapted to AIDL) | ggml logs variant scores only under `GGML_LOG_DEBUG`, so a release build cannot tell which kernels it got. **Prerequisite for R-1, R-3, R-4 and the whole Planner.** `CPU_DISPATCH_ANALYSIS.md` §4 | **Nothing.** Three build facts are exported (`skein_llama_version`, `_default_backend`, `_vulkan_available`); nothing runtime | M (JNI + AIDL parcelable + tests) | Turns "CPU/Vulkan/kernel variant" from unknowable into asserted; enables every §5 test | Must be **allowlist-built** and privacy-safe (§12): ggml feature names and device descriptions only, never paths, model names or prompts | **M0/M1** |
| **OL-06** `useLegacyPackaging = true` so `nativeLibraryDir` is populated for `GGML_BACKEND_DL` | `app/build.gradle.kts#L48-L54` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Required only by the plugin model Skein does not use; un-compresses every native lib in the APK, working against `skein-jn3`'s ≤ 30 MB. `CPU_DISPATCH_ANALYSIS.md` §1 | `GGML_BACKEND_DL OFF`, one static `.so` | — | — | Adopting it would grow the APK and add a `dlopen`-by-path dependency for the isolated process | — |
| **OL-55** Field guidance: on Mali, tg is bandwidth-bound and CPU kernels can beat the GPU | `README.md#L100` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Maintainer's aggregated field experience, directly about Mali — Skein's dev device is Mali-G715. Consistent with the M0 smoke's flat 5.61 tok/s tg. `INFERENCE_PLANNER_PROPOSAL.md` §2 | `SKEIN_LLAMA_DEFAULT_BACKEND = "vulkan"` on the strength of one smoke run | — | Turns the backend default into a hypothesis `skein-5hr` must test | **Not a measurement.** Must never be cited as evidence in `docs/MEASUREMENTS.md` | — |

### 1.2 Memory (directive §8) and context (§7)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-07** `ctx_params.n_outputs_max = 1` | `smollm/src/main/cpp/LLMInference.cpp#L165-L173` | 5.1.1 / `e81091e` | Apache-2.0 | **ADOPT** | Default is `n_batch`; the pp graph reserves `n_ubatch × n_vocab × 4` of logits that Skein never reads (it already sets `batch.logits` last-only and samples with `-1`). `MEMORY_ANALYSIS.md` §2 | **Missing.** `newContext` never sets it; `n_batch = 512` | XS (1 line) | **~297 MiB** (Qwen 2.5, 151 936 vocab) to **~512 MiB** (Gemma-class, 262 144) per context | Must be raised if multi-token output per decode is ever wanted (speculative decoding, batched eval). Guard with a comment and R-6 | **M0** |
| **OL-08** `no_perf = true` on context and sampler chain | `smollm/src/main/cpp/LLMInference.cpp#L180`, `#L189` | 5.1.1 / `e81091e` | Apache-2.0 | **ADOPT** | llama.cpp defaults perf accounting **on**: a timing call per sampler invocation and a summary printed on free. `JNI_ANALYSIS.md` §6 | Both left at defaults | XS (2 lines) | Removes a per-token timing call; removes an unwanted log on free | Loses llama.cpp's built-in perf summary — Skein measures its own timings anyway | M1 |
| **OL-09** Warm-up: decode BOS once, then `llama_memory_clear` | `smollm/src/main/cpp/LLMInference.cpp#L237-L247` | 5.1.1 / `e81091e` | Apache-2.0 | **ADAPT** | Moves backend buffer allocation and Vulkan pipeline creation into "Loading…" instead of the user's first TTFT | None | S | Lower first-token latency; a load that fails on allocation fails at load, not mid-chat | Must run on the worker thread inside `submitBlocking`; the `llama_memory_clear` afterwards is **not** optional or the BOS pollutes turn 1 | M1 |
| **OL-10** `useMmap` default **off**, for all models, on a TTFT/page-eviction argument | `app/.../data/repository/SettingsRepository.kt#L247-L253` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | *Extends* PP-41: a second, independent rationale (evictable file-backed pages stall TTFT) for the same direction, applied unconditionally. `MEMORY_ANALYSIS.md` §4 | `useMmap = true` hard-coded at `InferenceService.kt:278` | S (plumbing) + M (measurement) | A measured default instead of an assumed one; feeds `skein-brwf` | mmap off costs ~1× model size in anonymous RSS. `LLAMA_LOAD_MODE_NONE` still works over the pinned fd, so isolation is unaffected — but the memory profile changes completely | M1/M2 |
| **OL-11** Opt-in Q8_0 KV cache (`type_k`/`type_v`) | `smollm/src/main/cpp/LLMInference.cpp#L174-L179` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Halves KV at long contexts, but relies on "llama.cpp enables flash attention automatically where supported" — PP-42's crash class (upstream #481) with the compatibility matrix replaced by hope | None; `skein-brwf` carries the stronger refusal | — | A known 2× KV lever for the Resource Planner | Do **not** take the implementation; `skein-brwf`'s "never quantized KV with flash attention on the shipped backends" must stand | M2 |
| **OL-12** `MemoryMonitor` polling `ActivityManager.MemoryInfo` every 3 s | `app/src/main/java/com/jegly/offlineLLM/utils/MemoryMonitor.kt` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | `availMem` is system-wide and includes reclaimable cache — a poor predictor of whether a specific allocation succeeds. Advisory only; nothing acts on it. `MEMORY_ANALYSIS.md` §6 | None | — | — | The real finding is the **absence** of an estimator: two reviewed upstreams, neither predicts memory before loading. That is `skein-f9zu`'s justification | — |
| **OL-13** Context cap: `min(GGUF-declared, file-size ladder)`, user override wins | `smollm/src/main/java/com/jegly/offlineLLM/smollm/SmolLM.kt#L53-L66` | 5.1.1 / `e81091e` | Apache-2.0 | **ADAPT** | The **four-step shape** (declared → computed ceiling → min → override) is right and Skein has no equivalent. The *ladder* (>2 GB→4096, else 8192; two rungs share a value) ignores RAM, layers, head dims and cache type. `MEMORY_ANALYSIS.md` §5 | `InferenceConfig.contextLengthCap = 16_384`, a constant | M (contract) + L (formula, `skein-f9zu`) | Stops allocating a theoretical maximum context on a device that cannot hold it | Copying the ladder looks finished and is not; must **report** the clamp, never apply it silently | M2 |
| **OL-14** `ctx_params.n_batch = contextSize` | `smollm/src/main/cpp/LLMInference.cpp#L158-L159` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | This is why OL-07 was needed. `n_batch` and `n_ctx` look interchangeable and are not | `n_batch = 512` explicit, `n_ubatch` clamped with a precondition comment (`skein_jni.cpp:479-482`) | — | — | Skein is already right; the risk is a future "simplification" that couples them | — |
| **OL-15** Context exhaustion as `throw std::runtime_error("context size reached")` | `smollm/src/main/cpp/LLMInference.cpp#L381-L385` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Refusing *before* `llama_decode` is right; a string the UI matches on is PP-67 exactly. Second independent instance | Typed `ErrorCode.CONTEXT_FULL` → `StopReasons.LENGTH` | — | Strengthens `skein-r8ah`'s sibling assertion: never string-match this | — | — |

### 1.3 KV cache and conversation (directive §9)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-16** `_prevLen` incremental templated-prefix reuse | `smollm/src/main/cpp/LLMInference.cpp#L294-L337`, `LLMInference.h#L39-L42` | 5.1.0+ / `e81091e` | Apache-2.0 | **ADAPT** | Removes quadratic re-prefill; three sub-details are right (BOS once via `add_special = _prevLen == 0`; `add_generation_prompt` differs between feed and checkpoint renders; character offsets). But it is a *chat-transcript prefix* cache, which RAG breaks. `KV_CACHE_ANALYSIS.md` §1, §4 | **None.** `generateInternal` decodes from `nPast = 0` every request | L (needs `skein-82g`'s block structure) | Turn-N TTFT stops being the whole conversation's prefill | Copying it wholesale constrains Skein to a chat architecture (§15). Key on a **digest** and split stable prefix from volatile suffix; prefix boundary must be a `ChatTemplating` segment boundary | M2 |
| **OL-17** Invalidate on shrink: `_prevLen > new_len` → `_prevLen = 0` + `llama_memory_clear` | `smollm/src/main/cpp/LLMInference.cpp#L329-L332` | 5.1.0+ / `e81091e` | Apache-2.0 | **ADAPT** | The fail-closed recovery is exactly right — a miss can only be slow, never wrong. The **trigger** is a length check standing in for a content check | None | S (with OL-16) | Correct handling of truncation, edits, regeneration and conversation switching, for free | A same-length edit or system-prompt swap passes the length check with a stale cache. Reachable in Skein (PP-68…PP-71); unreachable in OfflineLLM | M2 |
| **OL-18** `storeChats = false` → clear buffer, `_prevLen`, and memory every turn | `smollm/src/main/cpp/LLMInference.cpp#L279-L284` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | A clean single-turn mode | Skein gets it for free from the explicit `kvClear` recommended in `KV_CACHE_ANALYSIS.md` §3 | — | — | — | — |
| **OL-26** Model-family knowledge scattered across four layers (C++ role sniff, C++ stop list, Kotlin stop list, Kotlin regex scrub) | `LLMInference.cpp#L224-L233`, `#L291`, `#L402-L404`; `InferenceEngine.kt#L206-L236` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Four places, no shared constant; a new model family means four edits. The output regex also strips legitimate text containing those markers | `StopStringMatcher` takes its set from `req.sampling.stop`, per request | — | — | Skein is already right; PP-36's "minimise hard-coded model knowledge" is the rule to keep | — |

### 1.4 JNI and architecture (directive §3)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-19** `llama_log_set` capture of the first WARN/ERROR lines, folded into the thrown message | `smollm/src/main/cpp/LLMInference.cpp#L60-L89`, `#L96`, `#L153-L154`; `LLMInference.h#L53-L57` | 5.1.1 / `e81091e` | Apache-2.0 | **ADAPT** | llama.cpp collapses "not a GGUF", "truncated", "unknown arch" and "alloc failure" into one null return — Skein's own comment says so. Keeping the **first** lines is the non-obvious part (errors cascade specific→generic). `JNI_ANALYSIS.md` §5 | Pieces exist (`llama_log_set` glue, `LlamaLogRedactor`) but are not joined; the exception carries a constant string | S–M | `INVALID_MODEL` becomes "missing tensor blk.0.attn_q.weight". Highest-value usability item in the review | **Must route through `LlamaLogRedactor.redact` before the message crosses Binder** — spec §9's no-raw-content rule applies to exception text too. Cap the buffer (upstream: 512 chars) or a hostile GGUF amplifies memory | M1 |
| **OL-20** Raw `jlong` → pointer cast as the handle, at 8 entry points | `smollm/src/main/cpp/smollm.cpp#L176` (and `#L184`, `#L190`, `#L196`, `#L204`, `#L217`, `#L229`, `#L236`) | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Stale, wrong-kind or forged handles all dereference; several Kotlin call sites skip even the `!= 0L` check | Typed `HandleKind` registry, monotonic non-reused ids, mutex lookup, `IllegalStateException` on any miss (`handles.h`) | — | — | Skein is strictly better. Worth citing this tag in `handles.h`'s "WHY NOT RAW POINTERS" comment as the worked example | — |
| **OL-21** `LLAMA_BUILD_COMMON=OFF` because `common` carries HTTP/download code | `smollm/src/main/cpp/CMakeLists.txt#L62-L64`; `smollm/build.gradle.kts#L85-L87` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Two independent zero-network projects converging on the same flag for the same reason | Already set, plus `LLAMA_CURL`, `LLAMA_OPENSSL`, `LLAMA_SUBPROCESS`, `GGML_RPC`, `LLAMA_BUILD_UI`, `LLAMA_USE_PREBUILT_UI` — a strict superset | — | One citation in `native/llama/README.md` §6 | — | — |
| **OL-22** `tokenToPiece` fixed 256-byte buffer with the negative return clamped to 0 | `smollm/src/main/cpp/LLMInference.cpp#L31-L40` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | llama.cpp returns `-required_size`; clamping to 0 silently **drops the token**. Invisible failure | `tokenToPieceBytes` returns a length-correct byte array | — | — | Turn into R-10a: a full-vocabulary round-trip sweep | — |
| **OL-23** UTF-8 continuation buffering (`_cacheResponseTokens` + `_isValidUtf8`) | `smollm/src/main/cpp/LLMInference.cpp#L350-L377`, `#L431-L438` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Correct length-prefix walk, but **nothing ever flushes** — a genuinely invalid byte accumulates forever | `Utf8Buffer` + `flush()` on completion (`InferenceService.kt:456-458`) | — | Confirms the tail-flush path is load-bearing | — | — |
| **OL-24** Unload protocol: native `stop()` → cancel → **bounded join** → free | `app/.../ai/InferenceEngine.kt#L105-L124`; `SmolLM.kt#L187-L193` | 5.1.1 / `e81091e` | Apache-2.0 | **ADAPT** | The `withTimeoutOrNull(5_000) { join() }` before the native free is the step most implementations skip, and the bound is the part those that do it get wrong. Zeroing the handle before the free is the companion detail | `InferenceWorker.submitBlocking` serialises on one thread; the invariant is emergent, not asserted | S (as a test) | Removes a use-after-free class by construction rather than by luck | An unbounded join hangs unload on a stuck JNI call | M1 |
| **OL-25** `gguf_context` never freed; `_chatTemplate` `strdup` never freed; double-`strdup` per turn | `smollm/src/main/cpp/GGUFReader.cpp` (no `gguf_free`); `LLMInference.cpp#L221`, `#L415` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Per-load and per-turn leaks, unbounded across a session. Bounded in size (`no_alloc = true`) but includes full KV metadata | `handleCount()` is `map.size()` by construction | — | — | The transferable part is the **test**: R-8 (20 load/inspect/free cycles, handle count returns to baseline). PP-53 from a second direction | M1 |
| **OL-30** Deliberately **no** arch flags on the JNI wrapper translation unit | `smollm/src/main/cpp/CMakeLists.txt#L39-L41` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | A recorded negative result: *"arch flags on it never affected inference speed"* — the arithmetic is all in ggml | N/A | — | Saves someone tuning `-march` on `skein_jni.cpp` | — | — |

### 1.5 CPU dispatch (directive §4)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-27** `GGML_CPU_ALL_VARIANTS=ON` + `GGML_BACKEND_DL=ON` + `ggml_backend_load_all_from_path` | `smollm/build.gradle.kts#L92-L99`; `smollm/src/main/cpp/smollm.cpp#L12-L29` | 5.1.1 / `e81091e` | Apache-2.0 | **ADAPT** (simpler equivalent) | The ladder is **upstream llama.cpp** (seven `android_*` variants, `ggml/src/CMakeLists.txt#L533-L540`); upstream also forbids it without `GGML_BACKEND_DL`. The plugin model contradicts Skein's single-`.so` isolated-process posture, ×8 the reproducibility surface, and forces OL-06. **The simpler equivalent is one line: raise `GGML_CPU_ARM_ARCH`.** `CPU_DISPATCH_ANALYSIS.md` §2, §6 | `GGML_BACKEND_DL OFF`, `GGML_CPU_ALL_VARIANTS` unset, `GGML_CPU_ARM_ARCH` unset → `armv8-a`; ARM repack kernels **not compiled in** | XS (the flag) + M (measurement, `skein-5hr`) | The dotprod/i8mm quantized-matmul path exists at all. Tensor G3 is Armv9.0 — every kernel is available on the dev device | Raising the floor excludes older arm64 devices; the decision must record that. **Buys nothing until OL-01 lands** (`Vulkan_Host` would displace the repack buffer type) | M1/M2 (`skein-5hr`) |
| **OL-28** "A device showing only `NEON` fell back to the armv8.0 baseline and will be several times slower" | `README.md#L99-L103` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Maintainer's field characterisation of the exact configuration Skein ships today | `armv8-a` floor | — | Motivates `skein-5hr` with a magnitude estimate | Not a measurement; must not be cited as one | — |
| **OL-29** `n_threads` (generation, big cores) and `n_threads_batch` (prompt, all cores) as separate knobs | `smollm/src/main/cpp/LLMInference.cpp#L160-L164`; `ModelManager.kt#L180`; `SettingsRepository.kt#L76` | 5.1.1 / `e81091e` | Apache-2.0 | **ADOPT** | Prompt processing is compute-bound and scales across all cores; generation is memory-bound and prefers the big-core count. One knob split in two with a physical justification. `JNI_ANALYSIS.md` §7 | `params.n_threads_batch = n_threads` — one value for both (`skein_jni.cpp:483-484`) | S (one extra `jint` through `LoadRequest`) | Faster prompt processing on the CPU path without hurting generation | **Measuring it before OL-01 measures the Vulkan path, not the CPU one.** PP-59 gives the number; this gives the split — `skein-brwf` should carry both | M0/M1 |

### 1.6 GGUF import (directive §10)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-31** `.part` staging + declared-size agreement + atomic rename | `app/src/main/java/com/jegly/offlineLLM/ai/ModelManager.kt#L81-L131` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | The code comment is a field bug report: a truncated `.gguf` passes the magic check and fails at load. PP-14/PP-29/PP-30 independently | `<file>.tmp` + `ATOMIC_MOVE` + two digests + `0400`/`0500` (`ImmutableModelStore.kt:239-254`) | — | Corroborates `skein-r8ah` from a second codebase; **and** shows the missing upper bound (a provider declaring 900 GB fills `filesDir`) — confirming `SKEIN_HUB.md` §3.2's 16 GiB ceiling is necessary | — | — |
| **OL-32** Validation = four magic bytes, then straight to `gguf_init_from_file` | `ModelManager.kt#L281-L294`; `smollm/src/main/cpp/GGUFReader.cpp#L9-L12` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | No version, KV-count, tensor-count or length-vs-remaining checks. A malformed GGUF also *registers successfully* with default metadata (`catch (_: Exception) {}`, ctx = 2048) and fails later | `skein-hewz` designs the bounded pre-check; two digest gates already exist | — | — | Two reviewed upstreams, **neither** has a bounded pre-check — so `skein-hewz` has no reference implementation to borrow and `skein-wt92`'s twelve fixtures (PP-12) remain the only ready-made corpus | — |
| **OL-33** Identity = filename; collision = delete and overwrite | `ModelManager.kt#L116-L119`, `#L263-L270` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Two different models with the same filename silently become one. PP-72 specifies replace / keep-both / cancel | Manifest `id` (also the store directory name); content by digest | — | — | — | — |
| **OL-34** Copy into app-private storage; no persisted SAF URI anywhere | `ModelManager.kt#L22`, `#L95-L124` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Confirms PP-72 and `ImmutableModelStore`. A `content://` URI can be revoked, its backing file swapped, and an isolated process cannot resolve it | `ImmutableModelStore` | — | Second independent confirmation | Costs a full duplicate; `SKEIN_HUB.md` §5.2 addresses that for the Hub path | — |
| **OL-35** Model listing deletes the registry row when the file is missing | `ModelManager.kt#L229-L237` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | PP-73 (`pocketpal-ai#684`, "model disappears after app upgrade") implemented as intended behaviour. A container-path change on upgrade deregisters every model, so no re-anchoring is possible | `ImmutableModelStore` keys on a per-model directory under `filesDir` — re-anchoring is a prefix substitution | S (as an assertion) | — | **Adds an assertion to `skein-r8ah`**: a model whose recorded path no longer resolves is re-anchored or reported, never silently deregistered | M2 |

### 1.7 Provenance (directive §11)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-36** `ModelInfo`: no hash, no licence, no source, no revision, no quantization, no architecture | `app/.../data/local/entities/ModelInfo.kt`; `ModelManager.kt#L263-L270` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Two of §11's thirteen fields, both derived from the filesystem. PP-10/PP-15 taken to the limit: no expectation exists, so nothing can fail open *from* | `model-manifest.schema.json` requires `sha256`, `blake3`, `license.spdx`, `source.url`, `source.revision`, optional sigstore `attestation`; two gates on **every** load | — | — | Nothing to inherit — which also means §11's "verify licences independently" is trivially satisfied: OfflineLLM asserts no model licence anywhere | — |
| **OL-37** `SignatureVerifier`: APK signing-cert digest self-check at startup | `app/src/main/java/com/jegly/offlineLLM/utils/SignatureVerifier.kt` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | As anti-tamper it is theatre (patchable; the platform already blocks a differently-signed update) **and** it breaks FOSS rebuilds. The *mechanism* — `apkContentsSigners` + SHA-256 digest compare — is right for authenticating a **peer** package | `SKEIN_HUB.md` §2.4 already requires authenticating the sender | S | Ready-made shape for `HubPicker`; use the API ≥ 28 path only (`minSdk` 30) | Never self-check: an F-Droid or user rebuild would refuse to run. Prefer *reporting* the peer digest in diagnostics over *gating* on it | M2 |

### 1.8 Security (directive §13)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-38** `android:memtagMode="sync"` | `app/src/main/AndroidManifest.xml#L24` | 5.1.1 / `e81091e` | Apache-2.0 | **ADOPT** | ARM MTE in sync mode: a UAF or overflow faults at the access. The right mitigation for a process running a C++ tensor library over untrusted binary input. `SECURITY_ANALYSIS.md` §6 | **Declared nowhere** in Skein's manifests | XS (one attribute) | Memory-safety defence in depth on `:inference` / `:embedder` | `sync` costs measurable performance — **needs a measurement, not an assumption**, so do not land it silently before an M0 run. GrapheneOS's own memory-tagging controls: interaction **could not be determined** (no device access) | M1 |
| **OL-39** `filterTouchesWhenObscured`, `allowBackup=false`, `hasFragileUserData=true` | `app/src/main/AndroidManifest.xml#L20-L33`; toggle at `SettingsRepository.kt#L79` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Tapjacking mitigation is cheap and relevant to a vault-holding activity. `allowBackup` differs deliberately (Skein has `dataExtractionRules`) | `hasFragileUserData` matches; `filterTouchesWhenObscured` and `FLAG_SECURE` absent | XS | — | Belongs to the existing UI security baseline (spec §9 / `E3.I1`), not to this bead | — |
| **OL-40** Zero-network enforced by a manifest comment block | `app/src/main/AndroidManifest.xml#L13-L16` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | A convention, with no CI to enforce it — nothing stops a dependency merging `INTERNET` | `ManifestGuardTask` denylist + `ManifestPolicyTest` + three `tools:node="remove"` entries with per-permission justifications | — | Evidence that the build-time gate is worth its cost | — | — |
| **OL-41** Single process: untrusted GGUF parsed beside the chat DB and encrypted prefs | `AndroidManifest.xml` (no `<service>`); `GGUFReader.cpp#L10` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | PP-16 independently. **Two of two reviewed upstreams parse GGUF in the app process** | `:inference` with `isolatedProcess="true"`; `SKEIN_HUB.md` §3.3 moves inspection inside it | — | — | Skein's largest security differentiator found in this epic. The `llama_chat_apply_template` dependency on a model-supplied template string is what makes the isolation load-bearing rather than nice-to-have | — |
| **OL-42** `parse_special = true` over user content; `<start_of_turn>` in a query bypasses the template entirely | `smollm/src/main/cpp/LLMInference.cpp#L290-L294`, `#L333-L336` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | A user message containing `<start_of_turn>` becomes real control tokens. Limited to self-injection in a chat-only app; a **remote prompt-injection primitive** in any app that injects retrieved text. PP-33/PP-34, worse. `SECURITY_ANALYSIS.md` §4 | `ChatTemplating`: scaffold `parseSpecial=true`, content `parseSpecial=false`, no exemptions, fail-closed segmentation | — | — | **Confirms `skein-0ztk` by counterexample.** Add the named `ChatTemplatingTest` case (R-10b). A courtesy upstream report is appropriate; filing one is outside this bead | — |
| **OL-43** `sanitizePrompt(maxLength = 2048)` silent truncation | `app/src/main/java/com/jegly/offlineLLM/utils/SecurityUtils.kt#L15-L20` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Data loss dressed as a control; does not address the actual injection path | `ContextBudget` bounds by tokens against the real window and reports | — | — | — | — |
| **OL-44** `secureDelete`: overwrite with `SecureRandom` then delete | `SecurityUtils.kt#L34-L60`; README claim at `README.md#L166` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Overwriting a logical block does not overwrite the physical one on flash; on F2FS the write may land in a new segment. Burns write cycles. **Called from nowhere** — deletion uses plain `File.delete()` | Encryption at rest + key destruction | — | — | A README security claim with no implementation behind it and no effect if there were | — |
| **OL-45** EncryptedSharedPreferences + StrongBox request with a reflective `isStrongBoxBacked()` probe | `app/.../data/repository/SettingsRepository.kt#L84-L116` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Reports `"StrongBox"` on **system-feature presence**, regardless of whether the key got it — a diagnostic label that can lie | SQLCipher vault with its own KDF; no Jetpack Security dependency | — | Useful warning: *"Jetpack Security doesn't consistently expose StrongBox state across versions"* — relevant if any Skein surface claims hardware backing | — | — |

### 1.9 Build and reproducibility (directive §14)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-46** `-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON` | `smollm/build.gradle.kts#L82` | 5.1.1 / `e81091e` | Apache-2.0 | **ADOPT** (conditional) | PP-48 (`pocketpal-ai#512`, `SIGBUS` on 16 KiB-page devices) with the *remedy* attached. An NDK-level CMake variable, more idiomatic than a raw `-Wl,-z,max-page-size=16384` | Neither is set anywhere in `native/llama/`, `native/sqlite/`, `inference-service/build.gradle.kts` or `tools/ci/` | XS | Removes a `SIGBUS`-on-load class that looks exactly like an OOM | **Check before changing.** NDK r27 may already align by default; adding the flag means re-recording the `.so` hashes in `native/llama/README.md` §4. Amend `skein-xth5`, do not file a new bead | M1 |
| **OL-47** `-ffunction-sections -fdata-sections` + `-Wl,--gc-sections` | `smollm/src/main/cpp/CMakeLists.txt#L22-L37` | 5.1.1 / `e81091e` | Apache-2.0 | **ADAPT** | Skein whole-archives every llama.cpp/ggml static lib into one `.so`, so unreferenced functions are dead weight. Reachability-based, so it cannot restrict GGUF support; needs no patch; cannot affect Vulkan performance | Not used; Skein uses a version script instead | S + M (verification) | Some code-size reduction — **off the ~4.8 MiB of code, not the ~19.3 MiB of referenced SPIR-V**, so expect single-digit MiB at best | Must re-verify the double-build hash (`native/llama/README.md` §4). **Do not take `-flto`** (Skein sets `GGML_LTO OFF`; LTO is a reproducibility hazard) or `-fvisibility=hidden` (conflicts with the version script) | M2 |
| **OL-48** CMake version = "newest present in the SDK", by directory scan | `smollm/build.gradle.kts#L21-L35` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | The toolchain depends on what the developer installed last. Convenient; the opposite of reproducible | AGP-pinned CMake; `PINNED_COMMIT` verified at configure time | — | — | The disqualifying item: the gap is the toolchain *discovery model*, not a few flags | — |
| **OL-49** SPIRV-Headers cloned at unpinned `HEAD`; `/usr/bin/gcc` hard-coded; `linux-x86_64` glslc path | `README.md#L131`; `smollm/build.gradle.kts#L57-L58`, `#L109` | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Two builds of the same tag on different days can use different SPIR-V grammar headers. The hard-coded host paths also make macOS builds impossible | Both Khronos repos are pinned submodules; `_NDK_HOST_TAG` detection | — | — | — | — |
| **OL-50** `vulkan-host-toolchain.cmake` for `vulkan-shaders-gen`; SPIRV include via a cache var rather than `cppFlags` | `smollm/build.gradle.kts#L37-L61`; `smollm/src/main/cpp/CMakeLists.txt#L4-L12` (added in 5.1.1) | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Corroborates two Skein solutions independently, with an argument worth recording: `cppFlags` becomes `CMAKE_CXX_FLAGS`, which is whitespace-split (breaks on paths with spaces) and applies globally | `native/llama/CMakeLists.txt:555-562` — the same fix | — | One citation in `native/llama/README.md` §5 | — | — |
| **OL-51** No `distributionSha256Sum` on the Gradle wrapper; no CI at all | `gradle/wrapper/gradle-wrapper.properties`; `.github/` = `FUNDING.yml` only | 5.1.1 / `e81091e` | Apache-2.0 | **REJECT** | Nothing verifies the build tool, and nothing enforces the manifest convention (OL-40) | `.github/workflows/ci.yml` with symbol and ELF guards | — | — | — | — |
| **OL-52** F-Droid metadata pinned at 5.0.2 / versionCode 7 while the repo is 5.1.1 / 9 | `com.jegly.offlineLLM.yml` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | The Vulkan-era releases are **not** F-Droid-built; the recipe predates the Khronos-header requirement and would fail at configure | N/A | — | Provenance caution: the published 5.1.1 APK is not independently reproduced — **every finding in this directory is from source, none from a binary** | — | — |
| **OL-53** ~1 400 shaders shipped uncut; no trimming technique anywhere | `README.md#L141`; no shader-subsetting option in `smollm/build.gradle.kts` or `smollm/src/main/cpp/CMakeLists.txt` | 5.1.1 / `e81091e` | Apache-2.0 | **REFERENCE** | Directly answers §14: **no upstream precedent exists** for `native/llama/README.md` §7 option 2 (trim the shader set) | 24.10 MiB `.so`, ~19.3 MiB SPIR-V | — | Option 2 is bespoke engineering with unbounded maintenance cost. Weak but real support for §7's tentative choice — raise the budget, keep Vulkan | — | — |

### 1.10 Benchmarking and planning (directive §6, §12)

| Feature | OfflineLLM source path | Commit/tag | Licence | Class | Reasoning | Skein equivalent / current status | Difficulty | Expected benefit | Risks | Milestone |
|---|---|---|---|---|---|---|---|---|---|---|
| **OL-54** `benchModel(pp, tg, pl, nr)` in-process bench with per-phase memory clears and a reported σ | `smollm/src/main/cpp/LLMInference.cpp#L462-L536`; `smollm.cpp#L233-L239` | 5.1.1 / `e81091e` | Apache-2.0 | **ADAPT** | PP-51 exactly, with σ computed rather than discarded. Three weaknesses: no thermal gate, returns a formatted string, and the tg loop reuses token id 0. `INFERENCE_PLANNER_PROPOSAL.md` §4 | None in-app; `tools/m0-benchmark` is the (stricter) authority for `docs/MEASUREMENTS.md` | M | An in-app measurement primitive the Planner can use to fill a profile table | Must gate on `ThermalStatusSource` before **and** after and discard a run that crossed a threshold; must return a parcelable, not a string. Must never become telemetry (PP-54) | M2 |

---

## 2. Cross-references to `docs/research/POCKETPAL_RECON.md`

Not restated; see `README.md`'s index table. The rows above that change a PP finding's content:

| PP-id | Change | Row |
|---|---|---|
| PP-41 | **Extended** — a second, independent rationale (page eviction → TTFT stall) and an *unconditional* mmap-off default, not just for repackable quants | OL-10 |
| PP-48 | **Remedy added** — `skein-xth5` gains the NDK-idiomatic fix, and a "check before changing" sequence | OL-46 |
| PP-59 | **Extended** — the thread *split* (`n_threads_batch` = all cores) beside PP-59's thread *count* | OL-29 |
| PP-73 | **Assertion added** to `skein-r8ah` — re-anchor or report, never silently deregister | OL-35 |
| PP-11 / PP-16 | **Confirmed, no implementation available** — neither reviewed upstream has a bounded pre-check, so `skein-hewz` has nothing to borrow and `skein-wt92`'s fixtures remain the only corpus | OL-32, OL-41 |
| PP-45 / PP-46 | **Ordering constraint added** — `skein-f9zu` must fit *after* OL-07 and OL-01, or it encodes the bug | OL-07, OL-01 |
| PP-42 | **Confirmed** — a second project ships quantized KV on "llama.cpp enables FA automatically"; `skein-brwf`'s refusal stands | OL-11 |
| PP-55 | **Remedy found** — a 12-line CPU retry | OL-04 |
| PP-67 | **Second instance** — context-full as a matched string | OL-15 |
| PP-51 | **Confirmed** — same bench shape, no thermal gate; Skein's harness stays stricter | OL-54 |

---

## 3. Proposed beads (NOT filed — §17: *"Do not file beads; propose them"*)

Milestone and tier per proposal. Existing beads are named where an **amendment** is proposed
rather than a new bead; amendments are preferable and are listed first.

### 3.1 Amendments to existing beads

| Bead | Amendment | Rows | Tier |
|---|---|---|---|
| `skein-brwf` | Add OL-29's `n_threads_batch` split (all cores for prompt, big cores for generation) beside PP-59's count; add OL-10's unconditional mmap-off as a case to **measure**, and note that `useMmap` must become a `LoadRequest` field before either rule can be expressed | OL-29, OL-10 | sonnet |
| `skein-xth5` | Add `-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON` as the preferred remedy; sequence as **check first, add the flag only on failure**, then re-record the `.so` hashes in `native/llama/README.md` §4 | OL-46 | haiku |
| `skein-r8ah` | Add a fourth assertion: a model whose recorded path no longer resolves is re-anchored or reported, **never silently deregistered** (PP-73 / OL-35) | OL-35 | sonnet |
| `skein-f9zu` | Add the ordering constraint — fit the estimator only **after** OL-07 and OL-01, against R-12's point D; model the compute-buffer term as a function of `n_outputs_max`, not as a measurement | OL-07, OL-01 | sonnet |
| `skein-hewz` | Note that neither reviewed upstream has a bounded pre-check, so there is no reference implementation; `skein-wt92`'s twelve fixtures remain the only ready-made corpus | OL-32 | sonnet |
| `skein-5hr` | Record that raising `GGML_CPU_ARM_ARCH` **buys nothing until OL-01 lands**, and that the Termux CPU cell is an upper bound on the APK, not a prediction | OL-27, OL-28, OL-01 | — |

### 3.2 New beads proposed

| # | Proposed bead | Rows | Milestone | Tier | Notes |
|---|---|---|---|---|---|
| B-1 | **CPU-only means CPU-only**: restrict `params.devices` to CPU+ACCEL at `nGpuLayers <= 0` in both JNI load entry points; add R-1's positive/negative control pair | OL-01 | **M0** | sonnet | Escalation E-2. Independent implementation from `llama.h` preferred over copying — see `MODEL_PROVENANCE_ANALYSIS.md` §4 |
| B-2 | **Cap the logits reservation**: `params.n_outputs_max = 1` in `newContext`, with the guard comment and R-6 (device assertion + CI source check) | OL-07 | **M0** | haiku | Escalation E-3. One line + one test |
| B-3 | **Backend and CPU-feature diagnostic over AIDL**: `IInferenceService.backendReport()`, allowlist-built, privacy-safe per §12 | OL-05 | **M0/M1** | sonnet | Prerequisite for R-1, R-3, R-4 and the Planner |
| B-4 | **Third benchmark cell** `cpu-on-vulkan-build` in `tools/m0-benchmark`, plus `GGML_OP_OFFLOAD_MIN_BATCH` support for one cell (R-2, R-5) | OL-03 | M1 | sonnet | Harness addition only; no existing cell or target changes |
| B-5 | **Actionable model-load errors**: capture the first llama.cpp WARN/ERROR lines, cap them, **redact** them, fold them into `LlamaException` | OL-19 | M1 | sonnet | Must preserve spec §9's no-raw-content rule across Binder |
| B-6 | **Native-layer small wins**: `no_perf` on context and sampler; warm-up decode + `llama_memory_clear` | OL-08, OL-09 | M1 | haiku | Three files, no design |
| B-7 | **Lifecycle assertions**: R-8 (20-cycle handle/RSS leak), R-9 (bounded join before native free), R-7 (KV does not survive a request, plus the explicit `kvClear`) | OL-25, OL-24 | M1 | sonnet | PP-53 from a second direction |
| B-8 | **Token and control-token containment tests**: R-10a full-vocabulary `tokenToPieceBytes` round-trip; R-10b `ChatTemplatingTest` case asserting content cannot forge a control token | OL-22, OL-42 | M1 | haiku | R-10b cites the upstream exploit permalink |
| B-9 | **`memtagMode="sync"` on `:inference` / `:embedder`**, with a sync-vs-async measurement and a GrapheneOS interaction check | OL-38 | M1 | sonnet | Needs device access; pairs with `skein-k3b2` |
| B-10 | **Resource Planner contract**: `min(declared, computed, override)` with a reported verdict, consuming `skein-f9zu`'s formula and `skein-hewz`'s metadata | OL-13, OL-11 | M2 | opus | Design first; `INFERENCE_PLANNER_PROPOSAL.md` §6 |
| B-11 | **Prefix-cache design for RAG prompts**: stable-prefix/volatile-suffix split, digest-keyed, segment-aligned — recorded **before** the prompt layout is frozen | OL-16, OL-17 | M2 | opus | `KV_CACHE_ANALYSIS.md` §4. The layout decision is the expensive one to reverse |
| B-12 | **In-app bench primitive** with a thermal gate and a typed result, feeding a profile table | OL-54 | M2 | sonnet | Never telemetry (PP-54) |
| B-13 | **Native size trim**: `-ffunction-sections -fdata-sections -Wl,--gc-sections`, with a double-build hash re-verification and a measured saving | OL-47 | M2 | sonnet | Explicitly **not** `-flto`, **not** `-fvisibility=hidden` |
| B-14 | **Sampling golden-output tripwire** across llama.cpp pin bumps (R-11), golden file beside `native/llama/PINNED_COMMIT` | OL-19 context / `JNI_ANALYSIS.md` §6 | M2 | haiku | A human-decision tripwire, not a correctness oracle |

**M0-scoped: B-1, B-2, B-3** (B-3 is M0/M1 and is a prerequisite for *verifying* B-1). Everything
else is M1 or later. See `EXECUTIVE_RECOMMENDATION.md` question 10.
