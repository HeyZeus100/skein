package app.skein.core.model

/**
 * Placeholder verifying that `:core:model` compiles as a pure Kotlin/JVM module
 * (no Android imports). Real contracts (InferenceEngine, VaultRepository, etc.)
 * land in later E0/E1 issues.
 */
object Placeholder
